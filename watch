#!/bin/sh

echo "About to watch; please have inotifywait and git..."

inotifywait -m ./ -e CLOSE_WRITE |
  while read path action file; do
    echo "$path" "$action" "$file"

    git add .
    git commit --no-gpg-sign -m "auto-commit"
  done